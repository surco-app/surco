node_modules
out
dist
.DS_Store
*.log
*.tsbuildinfo
.claude/worktrees
.superpowers
.env
.env.local
.vercel

# Media dropped in the repo root — source recordings, not source code.
*.mkv
*.mp4
# …but the landing page's own clips ship with the site.
!apps/web/public/*.mp4
